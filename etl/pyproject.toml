[project]
name = "etl"
version = "0.1.0"
description = "ETL pipeline in Python to automate the integration of these data into PostgreSQL database"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.0.0",
    "psycopg2-binary>=2.9.0",
    "sqlalchemy>=2.0.0",
    "python-dotenv>=1.0.0",
]
